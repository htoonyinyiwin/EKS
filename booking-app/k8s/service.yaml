apiVersion: v1
kind: Service
metadata:
  name: booking-app
  namespace: booking-app
spec:
  selector:
    app: booking-app
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
